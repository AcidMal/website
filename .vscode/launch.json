{
  "version": "0.2.0",
  "configurations": [
    {
      "command": "npm run build && npm run preview",
      "name": "Development server",
      "request": "launch",
      "type": "node-terminal"
    }
  ]
}
